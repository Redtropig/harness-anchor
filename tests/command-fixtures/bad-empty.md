---
description: Fixture whose allowed-tools value is empty — must be rejected.
allowed-tools:
---

# /bad-empty (fixture)

Declares the key but provides no tools. An empty allow-list is almost certainly a
mistake, so it must fail validation.
