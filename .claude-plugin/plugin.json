{
  "name": "harness-anchor",
  "description": "Warn-only, zero-dependency runtime harness layer for Claude Code (companion to superpowers): persistent state, scope control (prompt + action side), Default-FAIL verification & coverage gates, entropy governance (golden-rules + drift detection), and first-class C/C++ engineering (CMake/Meson/Make/Bazel + clang-tidy + sanitizers).",
  "version": "0.10.0",
  "author": {
    "name": "redtropig"
  },
  "license": "MIT",
  "keywords": [
    "harness",
    "agent",
    "state",
    "verification",
    "scope-control",
    "cpp",
    "superpowers",
    "context-engineering"
  ]
}
